using UnityEditor;

public class YourEditorScript
{
    /*[MenuItem("YourMenu/Run Your Method")]
    public static void RunYourMethod()
    {
        if (!EditorApplication.isPlaying)
        {
            SO_ItemList itemList = new SO_ItemList(); // Tạo một đối tượng của class chứa hàm bạn muốn chạy
            itemList.SaveToJson(); // Gọi hàm bạn muốn chạy
        }
        else
        {
            // Nếu đang chạy game, bạn có thể hiển thị thông báo hoặc không làm gì cả
        }
    }*/
}